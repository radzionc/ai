{
  "name": "@product/grammar",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "build": "esbuild index.ts --bundle --platform=node --target=node22 --outfile=dist/index.js"
  },
  "dependencies": {
    "openai": "^4.77.0"
  },
  "devDependencies": {
    "esbuild": "^0.24.2"
  }
}
